// constants.dart
import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Colors.red;
  static const accentColor = Colors.blue;
}

class ApiUrls {
  static const baseUrl = 'https://api.example.com';
  static const loginEndpoint = '/login';
// Add other endpoint constants here
}

class AppStrings {
  static const appTitle = 'Artifact Matcher App';
  static const selectImages = 'Select Images';
  static const scanImages = 'Scan Images';
// Add other string constants here
}

class Dimensions {
  static const buttonHeight = 48.0;
  static const borderRadius = 12.0;
// Add other dimension constants here
}

// You can also add other constants like numeric values, paddings, etc.
